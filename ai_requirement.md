# MA平台AI Copilot完整能力清单（海外对标HubSpot, Braze, Iterable + 国内瓴羊/企点的落地形态）
> 底层前提：AI Copilot不是独立系统，嵌入MA各个模块；**AI只生成建议/草稿，所有执行动作（上线旅程、批量触达）必须人工确认，底层旅程引擎、消息网关、合规规则引擎仍然负责最终执行**。
> 已聊过：**RAG混合检索、文案生成、自然语言生成Journey画布**；下面把剩余能力补齐，每个能力附带：功能介绍 + 完整业务流程、输入输出、依赖模块。

## 前置公共底座（所有AI能力共享）
1. AI服务层：Prompt管理、模型路由、**混合检索RAG（公共知识库+租户私有知识库）**、PII脱敏、AI审计日志、熔断降级
2. 底层MA底座：CDP用户数据、标签/人群、旅程状态机、多渠道消息网关、合规许可引擎、报表指标
3. 安全约束：租户隔离；原始PII不进LLM；所有AI操作留审计日志；AI输出必须经过规则校验

---

## 能力1：多渠道营销文案生成（最早上线，RAG驱动）
### 功能描述
运营输入营销场景、目标人群、营销卖点、渠道类型（Email/SMS/Push/In-App），AI一次性生成多版文案，自动适配各渠道字数限制、退订/合规话术；支持改写、润色、风格切换、A/B变体生成。
> RAG在这里召回：同场景历史高转化模板、渠道合规规则、行业最佳实践。

### 完整流程
1. 运营输入：场景=购物车放弃召回；人群=APP加购未下单用户；卖点=10%折扣；渠道=SMS+Push+Email
2. Query预处理：提取关键词，送入**混合检索（BM25+向量）**
   - 公共知识库：召回海外TCPA/CAN-SPAM/GDPR渠道合规规则、通用模板规范
   - 租户私有知识库：召回该租户历史购物车放弃的优质短信/邮件模板
3. 检索片段 + 系统Prompt（渠道字数限制、合规要求）一起送入LLM
4. LLM输出多版本文案草稿
5. **后端规则引擎校验**：
   - SMS：检查是否符合TCPA，opt-in许可提示、退订文案；字符长度
   - Email：检查CAN-SPAM要求（物理地址、退订链接占位符）
6. 输出给运营预览，支持一键插入模板编辑器；运营可修改、保存；**保存记录AI版本、检索上下文、人工修改日志（AI审计）**
7. 可选：AI评估文案质量，预估打开/点击率（基于历史数据）

### 输入
场景描述、人群简述、卖点、渠道、风格要求
### 输出
多渠道多版本文案草稿 + 合规提示 + 预估效果参考

---

## 能力2：自然语言生成营销旅程 Journey（一句话生成画布）
### 功能描述
自然语言描述业务目标，AI自动拆解为旅程画布节点（触发、等待、分支、触达、退出），自动填充消息模板占位符、分支条件；生成后运营在画布中可视化编辑。

### 完整流程
1. 运营输入：
> 用户放弃购物车，立即发送In-App消息；等待24小时，如果没有点击，发送Push；再等待48小时仍未转化，短信兜底；一旦用户下单，立刻退出旅程。欧盟用户不要发送营销短信。
2. Query解析：LLM理解事件触发、等待时长、分支条件、渠道、区域合规约束
3. RAG混合检索：召回**旅程最佳实践、节点定义、渠道约束、区域合规规则**
4. LLM输出结构化Journey DSL（平台内部画布描述语言，不是直接渲染画布）
5. 后端规则校验引擎校验DSL：
   - 校验节点合法性：不存在非法等待、死循环
   - 校验合规约束：欧盟用户禁止营销SMS，自动增加人群分支
   - 校验通道可用性
6. 把DSL渲染成可视化旅程画布，消息节点自动挂载AI生成文案占位符
7. 运营可视化编辑、修改节点、补充文案；人工确认后，旅程才可以上线；AI不能直接上线旅程

### 输入
自然语言描述旅程逻辑、区域/人群约束
### 输出
旅程结构化DSL + 可视化画布预览 + 风险提示（例如：短信需要校验用户opt-in许可）

---

## 能力3：自然语言人群圈选 NL2DSL（自然语言转人群查询条件）
### 功能描述
运营用自然语言描述人群，AI翻译成平台人群查询DSL，自动圈选人群、预估人群规模，展示人群标签条件预览；适合复杂人群，降低运营学习标签语法成本。
> 当标签体系庞大时，可搭配轻量标签图谱做标签联想；Phase1只用Prompt内置标签字典+RAG检索标签说明文档。

### 完整流程
1. 运营输入：选出过去30天浏览商品但未下单，并且已同意接收营销邮件，没有收到过任何营销消息的欧盟用户
2. LLM解析意图，识别实体：时间范围、行为事件、许可状态、地区
3. RAG检索：标签/事件定义文档，确认标签名称、可用算子
4. LLM生成平台内部人群查询DSL
5. 后端校验：
   - DSL语法校验，标签/事件是否真实存在
   - 人群预估，返回预估人数，防止误生成超大人群
   - 可视化展示所有筛选条件
6. 运营可以修改条件、预览人群画像；确认后保存人群，用于旅程/批量投放
> 安全：AI生成的人群**不会自动执行投放**，必须人工确认。

### 输入
自然语言人群描述
### 输出
人群查询DSL、可视化筛选条件、人群预估数量、人群画像摘要

---

## 能力4：旅程诊断 & AI优化建议（存量旅程的智能分析）
### 功能描述
对已经上线/历史运行完的旅程做诊断，基于旅程漏斗数据，自动定位流失节点，给出优化建议：调整等待时长、更换触达渠道、优化文案、调整人群、增加A/B分支。

### 完整流程
1. 运营选择一个已上线旅程，触发AI诊断
2. 系统拉取该旅程的全链路指标：每个节点进入人数、流失率、送达/点击/转化数据；拉取该旅程使用的人群、渠道、消息模板
3. RAG检索：召回同类场景优秀旅程案例、运营最佳实践
4. LLM接收：旅程漏斗数据 + 旅程配置 + 参考案例
5. LLM输出诊断报告：
   - 问题定位：24h Push节点点击率极低，大量用户流失
   - 根因推测：该人群Push打开率历史偏低
   - 优化建议：优先改为In-App，Push改为备选；调整等待时长；新增A/B文案测试
6. 运营查看报告，可以一键把建议**应用为画布草稿**（不会直接修改正在运行的旅程）；人工审核后再发布新版本

### 输入
已存在的旅程ID + 历史运行指标
### 输出
旅程诊断报告、问题定位、优化建议，可生成旅程草稿

---

## 能力5：自然语言指标查询 / 智能报表问答（Text2SQL + RAG）
### 功能描述
运营用自然语言查询营销指标，不需要写SQL或者在报表里层层筛选，AI翻译成SQL查询指标，生成图表+自然语言解读，自动做归因。

### 完整流程
1. 用户提问：过去30天购物车放弃旅程，Email和Push渠道的点击率对比，和上个月对比变化
2. LLM理解指标、时间范围、维度（渠道）
3. RAG检索：指标字典、数据仓库表结构、字段说明
4. LLM生成SQL语句
5. SQL安全校验（防止越权、大查询），执行查询，拿到结果数据集
6. LLM基于数据集生成文字解读，指出涨跌原因
7. 返回图表+文字结论，支持导出报表

> 注意：这部分RAG检索的是**指标字典、表结构文档**，不是营销模板。

### 输入
自然语言指标提问
### 输出
查询结果、可视化图表、指标解读摘要

---

## 能力6：人群洞察分析（人群画像智能解读）
### 功能描述
选定一个人群，AI自动分析人群特征，提炼用户画像、行为偏好、触达偏好、风险点（许可状态、投诉概率），输出洞察报告，辅助运营选择渠道和文案策略。

### 完整流程
1. 选定人群，系统拉取人群统计标签、行为分布、历史触达指标
2. RAG检索：同类人群运营案例
3. LLM接收人群统计摘要，生成自然语言洞察：
> 该人群75%来自移动端，80%用户偏好In-App消息，短信打开率很低；欧盟占比高，全部需要校验email opt-in许可；流失风险偏高，建议使用温和文案。
4. 输出洞察报告，可直接用于生成文案/旅程的参考上下文

### 输入
人群ID
### 输出
人群画像洞察报告、渠道/文案策略建议

---

## 能力7：A/B实验智能助手（实验设计、结果解读）
### 功能描述
AI辅助设计旅程/消息A/B测试方案；实验结束自动解读实验显著性，判断哪个版本胜出，并给出后续迭代建议。
- 实验设计：AI建议分组策略、变量（文案、触达渠道、发送时机）、样本量、实验时长
- 实验解读：实验结束，输入指标数据，AI判断是否统计显著，解读差异原因

### 完整流程（实验解读）
1. 运营选择A/B实验ID
2. 系统拉取两组实验的指标、样本量、实验配置
3. RAG检索：A/B测试最佳实践、统计规则文档
4. LLM分析：判断版本差异是否显著，解读文案/渠道带来的差异
5. 输出结论，建议是否把优胜版本全量上线

### 输入
A/B实验配置+指标数据
### 输出
实验结论、显著性判断、迭代建议

---

## 能力8：智能内容/合规审查（AI预审文案、旅程）
> 可以嵌入文案编辑器、旅程保存前，作为预审环节，配合规则引擎。
### 功能描述
AI检查营销文案、旅程配置是否符合目标区域法规（GDPR/TCPA/CAN-SPAM），识别高风险内容、缺失退订、缺少opt-in校验等问题，给出修改提示。
> 规则引擎做主校验，AI做软补充，识别规则引擎难以覆盖的语义风险。

### 完整流程
1. 运营编辑完消息文案 / 完成旅程画布，点击AI预审
2. RAG检索当前租户所在区域的合规文档
3. LLM扫描文案、旅程配置
4. 识别风险：短信缺少opt-in声明；邮件缺少退订占位符；旅程对欧盟用户设置营销短信
5. 返回风险清单、风险等级、修改建议；**仍然以底层规则引擎的拦截作为最终防线**

---

# 所有AI能力的共性约束（海外客户重点关注）
1. **AI无法直接上线旅程、发送消息**，所有AI输出都是草稿/建议，必须人工审核确认；底层合规引擎和许可引擎是最终拦截层。
2. **全链路审计日志**：每一次AI对话、用户query、检索命中的RAG片段、LLM输出、人工修改记录全部持久化保存，满足GDPR/AI Act审计追溯。
3. **RAG租户隔离**：检索时强制带上tenant_id，只能检索公共知识库+当前租户私有知识库，禁止跨租户召回模板/案例。
4. PII脱敏：任何进入RAG chunk、进入LLM上下文的文本，必须把真实手机号、邮箱替换为模板变量 `${email}` `${phone}`。
5. 可降级：AI服务异常时，Copilot入口禁用，原有MA所有功能（旅程运行、消息发送、人群圈选）完全不受影响。

# 能力分层落地 roadmap（对标海外SaaS MA厂商）
- Phase1：文案生成 + NL2DSL人群圈选 + RAG混合检索（MVP，优先上线）
- Phase2：一句话生成Journey画布 + AI合规预审 + 人群洞察
- Phase3：旅程诊断优化 + Text2SQL自然语言问数
- Phase4：A/B实验智能助手、智能时机/通道推荐（离线预测模型，LLM做解读）

如果你需要，我可以把这8个能力整理成：
1. 产品功能PRD模块清单
2. 端到端时序图（例如一句话生成Journey完整时序）
3. 每个能力的Prompt模板样例

需要我整理哪一部分？